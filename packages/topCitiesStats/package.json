{
  "name": "update-top-cities",
  "version": "1.0.0",
  "dependencies": {
    "dotenv": "^10.0.0",
    "mysql2": "^2.3.0"
  },
  "engines": {
    "node": "18"
  },
  "scripts": {
    "dev": "functions-framework --target=updateTopCities --port=8080",
    "deploy": "gcloud functions deploy updateTopCities --runtime nodejs18 --trigger-http --max-instances=1 --region=us-central1 --timeout=60s --memory=128MB --entry-point=updateTopCities --project=peak-ranger-395021"
  }
}
