{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Python: Attach",
      "type": "python",
      "request": "attach",
      "port": 5678, // Make sure this port matches the port your debugger listens to
      "host": "localhost",
      "pathMappings": [
        {
          // This assumes your code is in <workspaceFolder>/packages/factorizationMachine
          "localRoot": "${workspaceFolder}/packages/factorizationMachine",
          "remoteRoot": "."
        }
      ]
    }
  ]
}
